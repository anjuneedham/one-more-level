# Copy to .env.production for a store build. Never commit real IDs.
# Without these three values, production builds keep using the mock ad
# provider and Google's test ad units (see src/services/config.ts).
VITE_ADMOB_APP_ID=
VITE_ADMOB_REWARDED_ID=
VITE_ADMOB_INTERSTITIAL_ID=
