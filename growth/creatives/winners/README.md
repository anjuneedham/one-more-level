# Winners

Creatives that reached a **KEEP** decision in `CAMPAIGN_TRACKER.md`, with enough real data behind them to trust it (see the minimum-data bar in that doc).

When a creative earns KEEP: copy its row from `data/creative-data.csv`, paste it below as a dated entry, and note *why* it worked (hook, format, timing) so the pattern can be repeated in future batches.

---

DATA REQUIRED — no creative has been tested yet.
